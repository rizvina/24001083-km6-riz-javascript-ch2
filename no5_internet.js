//soal no 5 internet

// Deklarasi array mycolor//
const myColor = ["Red", "Green", "White", "Black"];
// Convert array to string//
console.log(myColor?.toString()); 
// Menggabungkan ke dalam satu string, tanpa parameter/default sehingga dipisahkan tanda koma (default)//
console.log(myColor?.join()); 
// Menggabungkan ke dalam satu string dengan parameter + sebagai pemisah antar item//
console.log(myColor?.join("+")); 